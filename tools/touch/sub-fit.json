[
  { "probe": "JSON.stringify([...document.querySelectorAll('.case-sub')].map(e => ({t: e.textContent.slice(0,28), clipped: e.scrollHeight > e.clientHeight + 1, w: Math.round(e.getBoundingClientRect().width)})))", "as": "subs" }
]
