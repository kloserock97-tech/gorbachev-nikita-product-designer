/** Одна постоянная на весь сайт: с какой скоростью картинка догоняет прокрутку (1/с; за ~0,45 с — 95 % пути).
    Её берут мягкое колесо (ui/smoothScroll.ts: страницы кейсов, лёгкая версия) и скролл-история сцены
    (scene/HillScene.ts), поэтому главная и кейсы ощущаются одинаково. */
export const SCROLL_LAMBDA = 6.5;
